/**
 * ExecuteGetTalentGap - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: talent
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetTalentGap Use Case
 *
 * DDD: Application use case for get Gap.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetTalentGapInput, GetTalentGapOutput } from "../dto/gap.dto";
import type { ExecutionContextService, IdGeneratorService } from "@morphora/services/_shared/index.js";
import type { GapRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetTalentGap {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly gap: GapRepository
  ) {}

  async execute(input: GetTalentGapInput): Promise<GetTalentGapOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.tlnId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).gapId) {
      throw new ValidationError("gapId is required");
    }
    const gapId = (input as any).gapId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.gap.getTalentGap({ gapId: gapId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${gapId}`);
    }

    return entity as GetTalentGapOutput;
  }
}
