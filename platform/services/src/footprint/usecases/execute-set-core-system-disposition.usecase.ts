/**
 * ExecuteSetCoreSystemDisposition - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: footprint
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSetCoreSystemDisposition Use Case
 *
 * DDD: Application use case for update Disposition.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SetCoreSystemDispositionInput, SetCoreSystemDispositionOutput } from "../dto/disposition.dto";
import type { ExecutionContextService, IdGeneratorService } from "@morphora/services/_shared/index.js";
import type { DispositionRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteSetCoreSystemDisposition {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly disposition: DispositionRepository
  ) {}

  async execute(input: SetCoreSystemDispositionInput): Promise<SetCoreSystemDispositionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ftpId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).systemId) {
      throw new ValidationError("systemId is required");
    }
    const systemId = (input as any).systemId;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.disposition.setCoreSystemDisposition({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as SetCoreSystemDispositionOutput;
  }
}
