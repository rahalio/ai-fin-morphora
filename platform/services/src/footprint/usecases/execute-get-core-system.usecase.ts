/**
 * ExecuteGetCoreSystem - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: footprint
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetCoreSystem Use Case
 *
 * DDD: Application use case for get System.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetCoreSystemInput, GetCoreSystemOutput } from "../dto/system.dto";
import type { ExecutionContextService, IdGeneratorService } from "@morphora/services/_shared/index.js";
import type { SystemRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetCoreSystem {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly system: SystemRepository
  ) {}

  async execute(input: GetCoreSystemInput): Promise<GetCoreSystemOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ftpId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).systemId) {
      throw new ValidationError("systemId is required");
    }
    const systemId = (input as any).systemId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.system.getCoreSystem({ systemId: systemId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${systemId}`);
    }

    return entity as GetCoreSystemOutput;
  }
}
