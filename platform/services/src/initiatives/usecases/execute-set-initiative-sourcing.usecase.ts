/**
 * ExecuteSetInitiativeSourcing - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: initiatives
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSetInitiativeSourcing Use Case
 *
 * DDD: Application use case for update Sourcing.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SetInitiativeSourcingInput, SetInitiativeSourcingOutput } from "../dto/sourcing.dto";
import type { ExecutionContextService, IdGeneratorService } from "@morphora/services/_shared/index.js";
import type { SourcingRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteSetInitiativeSourcing {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly sourcing: SourcingRepository
  ) {}

  async execute(input: SetInitiativeSourcingInput): Promise<SetInitiativeSourcingOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.iniId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).initiativeId) {
      throw new ValidationError("initiativeId is required");
    }
    const initiativeId = (input as any).initiativeId;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.sourcing.setInitiativeSourcing({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as SetInitiativeSourcingOutput;
  }
}
