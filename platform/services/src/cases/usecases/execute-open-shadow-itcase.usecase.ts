/**
 * ExecuteOpenShadowITCase - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteOpenShadowITCase Use Case
 *
 * DDD: Application use case for get ShadowIt.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { OpenShadowITCaseInput, OpenShadowITCaseOutput } from "../dto/shadow-it.dto";
import type { ExecutionContextService, IdGeneratorService } from "@morphora/services/_shared/index.js";
import type { ShadowItRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteOpenShadowITCase {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly shadowIt: ShadowItRepository
  ) {}

  async execute(input: OpenShadowITCaseInput): Promise<OpenShadowITCaseOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cseId();

    // All field names extracted from OpenAPI spec: discoveredVia, systemName
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entityId = this.idGenerator.cseId();
    const entity = await this.shadowIt.openShadowITCase({ ...input, id: entityId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as OpenShadowITCaseOutput;
  }
}
