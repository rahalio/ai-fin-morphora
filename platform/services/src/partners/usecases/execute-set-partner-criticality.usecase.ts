/**
 * ExecuteSetPartnerCriticality - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: partners
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSetPartnerCriticality Use Case
 *
 * DDD: Application use case for update Criticality.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SetPartnerCriticalityInput, SetPartnerCriticalityOutput } from "../dto/criticality.dto";
import type { ExecutionContextService, IdGeneratorService } from "@morphora/services/_shared/index.js";
import type { CriticalityRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanUpdate } from "../policies/can-update.policy";
export class ExecuteSetPartnerCriticality {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly criticality: CriticalityRepository
  ) {}

  async execute(input: SetPartnerCriticalityInput): Promise<SetPartnerCriticalityOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prtId();

    // All field names extracted from OpenAPI spec
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).partnerId) {
      throw new ValidationError("partnerId is required");
    }
    const partnerId = (input as any).partnerId;
    // No get/list operation for this resource - call update directly
    // Check policy
    if (!CanUpdate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Update entity via repository (pass correlation ID for response meta)
    const updated = await this.criticality.setPartnerCriticality({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return updated as SetPartnerCriticalityOutput;
  }
}
