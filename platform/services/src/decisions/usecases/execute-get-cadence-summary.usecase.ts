/**
 * ExecuteGetCadenceSummary - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: decisions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetCadenceSummary Use Case
 *
 * DDD: Application use case for get Summary.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetCadenceSummaryInput, GetCadenceSummaryOutput } from "../dto/summary.dto";
import type { ExecutionContextService, IdGeneratorService } from "@morphora/services/_shared/index.js";
import type { SummaryRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetCadenceSummary {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly summary: SummaryRepository
  ) {}

  async execute(input: GetCadenceSummaryInput): Promise<GetCadenceSummaryOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.decId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.summary.getCadenceSummary({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetCadenceSummaryOutput;
  }
}
