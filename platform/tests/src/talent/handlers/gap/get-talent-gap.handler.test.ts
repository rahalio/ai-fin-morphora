/**
 * GetTalentGap handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: talent
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getTalentGap } from "@morphora/api-server/domains/talent/handlers";
import type { TalentDDDDependencies } from "@morphora/api-server/domains/talent";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@morphora/tests/shared";
import { TALENT_USE_CASES } from "../__factories__/use-cases.js";

describe("GetTalentGap", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TalentDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TalentDDDDependencies>(
      TALENT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetTalentGap, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { gapId: entity.id };

    await getTalentGap(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetTalentGap.execute).toHaveBeenCalledWith(expect.objectContaining({ gapId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { gapId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetTalentGap, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getTalentGap(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
