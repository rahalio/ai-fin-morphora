/**
 * GetDecision handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: decisions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDecision } from "@morphora/api-server/domains/decisions/handlers";
import type { DecisionsDDDDependencies } from "@morphora/api-server/domains/decisions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@morphora/tests/shared";
import { DECISIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDecision", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DecisionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DecisionsDDDDependencies>(
      DECISIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDecision, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { decisionId: entity.id };

    await getDecision(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDecision.execute).toHaveBeenCalledWith(expect.objectContaining({ decisionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { decisionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDecision, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDecision(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
