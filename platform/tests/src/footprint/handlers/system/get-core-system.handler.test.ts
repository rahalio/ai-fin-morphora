/**
 * GetCoreSystem handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: footprint
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCoreSystem } from "@morphora/api-server/domains/footprint/handlers";
import type { FootprintDDDDependencies } from "@morphora/api-server/domains/footprint";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@morphora/tests/shared";
import { FOOTPRINT_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCoreSystem", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: FootprintDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<FootprintDDDDependencies>(
      FOOTPRINT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCoreSystem, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { systemId: entity.id };

    await getCoreSystem(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCoreSystem.execute).toHaveBeenCalledWith(expect.objectContaining({ systemId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { systemId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetCoreSystem, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getCoreSystem(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
