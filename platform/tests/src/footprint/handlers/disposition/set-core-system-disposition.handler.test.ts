/**
 * SetCoreSystemDisposition handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: footprint
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { setCoreSystemDisposition } from "@morphora/api-server/domains/footprint/handlers";
import type { FootprintDDDDependencies } from "@morphora/api-server/domains/footprint";
import { createDisposition as createDispositionEntity } from "@morphora/factories/footprint";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@morphora/tests/shared";
import { FOOTPRINT_USE_CASES } from "../__factories__/use-cases.js";

describe("SetCoreSystemDisposition", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: FootprintDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<FootprintDDDDependencies>(
      FOOTPRINT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createDispositionEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { systemId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSetCoreSystemDisposition, "execute").mockResolvedValue(useCaseResult);

    await setCoreSystemDisposition(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSetCoreSystemDisposition.execute).toHaveBeenCalledWith(expect.objectContaining({ systemId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
