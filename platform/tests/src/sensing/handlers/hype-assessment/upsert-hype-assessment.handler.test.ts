/**
 * UpsertHypeAssessment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: sensing
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { upsertHypeAssessment } from "@morphora/api-server/domains/sensing/handlers";
import type { SensingDDDDependencies } from "@morphora/api-server/domains/sensing";
import { createHypeAssessment as createHypeAssessmentEntity } from "@morphora/factories/sensing";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@morphora/tests/shared";
import { SENSING_USE_CASES } from "../__factories__/use-cases.js";

describe("UpsertHypeAssessment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SensingDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SensingDDDDependencies>(
      SENSING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createHypeAssessmentEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { signalId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpsertHypeAssessment, "execute").mockResolvedValue(useCaseResult);

    await upsertHypeAssessment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpsertHypeAssessment.execute).toHaveBeenCalledWith(expect.objectContaining({ signalId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
