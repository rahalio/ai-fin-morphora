/**
 * Partner Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: partners
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { PartnerEntity } from "@morphora/core/partners/models/index.js";
import { partnersSchemas } from "@morphora/core/partners";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "prt";

export function createPartner(overrides: Partial<PartnerEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    criticality: overrides.criticality ?? "low",
    name: overrides.name ?? faker.person.fullName(),
    partnerId: overrides.partnerId ?? `prt_${ulid().toLowerCase()}`,
    partnerType: overrides.partnerType ?? faker.lorem.word(),
    spof: overrides.spof ?? faker.datatype.boolean(),
    ...overrides,
  };

  return partnersSchemas.Partner.parse(raw);
}

export function partnerBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createPartner(overrides));
}
