/**
 * GetExecPack handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: packs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getExecPack } from "@morphora/api-server/domains/packs/handlers";
import type { PacksDDDDependencies } from "@morphora/api-server/domains/packs";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@morphora/tests/shared";
import { PACKS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetExecPack", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PacksDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PacksDDDDependencies>(
      PACKS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetExecPack, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { packId: entity.id };

    await getExecPack(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetExecPack.execute).toHaveBeenCalledWith(expect.objectContaining({ packId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { packId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetExecPack, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getExecPack(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
