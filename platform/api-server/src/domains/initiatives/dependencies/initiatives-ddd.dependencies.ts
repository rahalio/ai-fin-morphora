/**
 * Initiatives DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: initiatives
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Initiatives Domain Module - Composition root
 *
 * DDD: Composition root for initiatives domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  InitiativeRepositoryAdapter,
  KillRepositoryAdapter,
  ScaleRepositoryAdapter,
  SourcingRepositoryAdapter,
} from "@morphora/adapters/initiatives";
import { getIdGeneratorService } from "@morphora/adapters";
import type { AdapterDynamoDBClient } from "@morphora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateChallengerInitiative,
  ExecuteGetChallengerInitiative,
  ExecuteKillInitiative,
  ExecuteListChallengerInitiatives,
  ExecuteScaleInitiative,
  ExecuteSetInitiativeSourcing,
} from "@morphora/services/initiatives/usecases";
// Ports (interfaces)
import type {
  InitiativeRepository,
  KillRepository,
  ScaleRepository,
  SourcingRepository,
} from "@morphora/services/initiatives/ports";

export interface InitiativesDomainModule {
  repos: {
    initiatives: InitiativeRepository;
    kills: KillRepository;
    scales: ScaleRepository;
    sourcings: SourcingRepository;
  };
  useCases: {
    initiatives: {
      create: ExecuteCreateChallengerInitiative;
      get: ExecuteGetChallengerInitiative;
      list: ExecuteListChallengerInitiatives;
    };
    kills: {
      get: ExecuteKillInitiative;
    };
    scales: {
      get: ExecuteScaleInitiative;
    };
    sourcings: {
      update: ExecuteSetInitiativeSourcing;
    };
  };
}

export function buildInitiativesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): InitiativesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      initiatives?: InitiativeRepository;
      kills?: KillRepository;
      scales?: ScaleRepository;
      sourcings?: SourcingRepository;
    } = {};

    obj.initiatives = new InitiativeRepositoryAdapter(dynamoClient);
    obj.kills = new KillRepositoryAdapter(dynamoClient);
    obj.scales = new ScaleRepositoryAdapter(dynamoClient);
    obj.sourcings = new SourcingRepositoryAdapter(dynamoClient);
    return {
      initiatives: obj.initiatives!,
      kills: obj.kills!,
      scales: obj.scales!,
      sourcings: obj.sourcings!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    initiatives: {
      create: new ExecuteCreateChallengerInitiative(executionContext, idGenerator, repos.initiatives),
      get: new ExecuteGetChallengerInitiative(executionContext, idGenerator, repos.initiatives),
      list: new ExecuteListChallengerInitiatives(executionContext, idGenerator, repos.initiatives),
    },
    kills: {
      get: new ExecuteKillInitiative(executionContext, idGenerator, repos.kills),
    },
    scales: {
      get: new ExecuteScaleInitiative(executionContext, idGenerator, repos.scales),
    },
    sourcings: {
      update: new ExecuteSetInitiativeSourcing(executionContext, idGenerator, repos.sourcings),
    },
  };
  return { repos, useCases: useCases as InitiativesDomainModule["useCases"] };
}
