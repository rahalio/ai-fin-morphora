/**
 * Footprint DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: footprint
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Footprint Domain Module - Composition root
 *
 * DDD: Composition root for footprint domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  DispositionRepositoryAdapter,
  SpeedTagRepositoryAdapter,
  SystemRepositoryAdapter,
} from "@morphora/adapters/footprint";
import { getIdGeneratorService } from "@morphora/adapters";
import type { AdapterDynamoDBClient } from "@morphora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateCoreSystem,
  ExecuteGetCoreSystem,
  ExecuteListCoreSystems,
  ExecuteSetCoreSystemDisposition,
  ExecuteSetCoreSystemSpeedTag,
} from "@morphora/services/footprint/usecases";
// Ports (interfaces)
import type {
  DispositionRepository,
  SpeedTagRepository,
  SystemRepository,
} from "@morphora/services/footprint/ports";

export interface FootprintDomainModule {
  repos: {
    dispositions: DispositionRepository;
    speedTags: SpeedTagRepository;
    systems: SystemRepository;
  };
  useCases: {
    dispositions: {
      update: ExecuteSetCoreSystemDisposition;
    };
    speedTags: {
      update: ExecuteSetCoreSystemSpeedTag;
    };
    systems: {
      create: ExecuteCreateCoreSystem;
      get: ExecuteGetCoreSystem;
      list: ExecuteListCoreSystems;
    };
  };
}

export function buildFootprintDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): FootprintDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      dispositions?: DispositionRepository;
      speedTags?: SpeedTagRepository;
      systems?: SystemRepository;
    } = {};

    obj.dispositions = new DispositionRepositoryAdapter(dynamoClient);
    obj.speedTags = new SpeedTagRepositoryAdapter(dynamoClient);
    obj.systems = new SystemRepositoryAdapter(dynamoClient);
    return {
      dispositions: obj.dispositions!,
      speedTags: obj.speedTags!,
      systems: obj.systems!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    dispositions: {
      update: new ExecuteSetCoreSystemDisposition(executionContext, idGenerator, repos.dispositions),
    },
    speedTags: {
      update: new ExecuteSetCoreSystemSpeedTag(executionContext, idGenerator, repos.speedTags),
    },
    systems: {
      create: new ExecuteCreateCoreSystem(executionContext, idGenerator, repos.systems),
      get: new ExecuteGetCoreSystem(executionContext, idGenerator, repos.systems),
      list: new ExecuteListCoreSystems(executionContext, idGenerator, repos.systems),
    },
  };
  return { repos, useCases: useCases as FootprintDomainModule["useCases"] };
}
